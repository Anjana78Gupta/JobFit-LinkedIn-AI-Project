# Fixtures

- `sample_jobs.json` -- two pre-scraped job listings in different raw shapes
  (different key naming conventions), used to exercise `JobNormalizer`'s
  alias-matching logic.
- Add a real sample resume (`.pdf` or `.docx`) here before running
  integration tests against `ParserPipeline`. Binary fixtures aren't
  generated by this scaffold -- drop in any anonymized resume file and
  reference its path from your test.
